% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gene_sets.R
\name{map_genes_to_features}
\alias{map_genes_to_features}
\title{Map gene symbols to feature IDs in an expression matrix}
\usage{
map_genes_to_features(
  gene_sets,
  gene_map,
  gene_col = "Gene",
  feature_col = "FeatureID",
  min_set_size = 3L
)
}
\arguments{
\item{gene_sets}{A named list of gene symbol vectors (as returned by
\code{\link{get_immune_gene_sets}}).}

\item{gene_map}{A data.frame with at least two columns: one containing
gene symbols and one containing feature IDs present in the expression
matrix. Default column names are "Gene" and "FeatureID".}

\item{gene_col}{Character, column name in \code{gene_map} containing gene symbols.}

\item{feature_col}{Character, column name in \code{gene_map} containing feature IDs.}

\item{min_set_size}{Integer, minimum number of mapped features required
to retain a gene set. Default: 3.}
}
\value{
A named list of character vectors, each containing feature IDs
that match to the expression matrix.
}
\description{
Given a list of gene sets (gene symbols) and a mapping table, this
function maps each gene to its corresponding feature ID in the
expression matrix. Only features present in the expression matrix
are retained, and gene sets with too few mapped features are
optionally dropped.
}
\examples{
gs <- get_immune_gene_sets()
# Example gene-to-protein mapping
mapping <- data.frame(
  Gene = c("B2M", "HLA-A", "HLA-B", "CD8A"),
  Protein = c("P61769", "P04439", "P01889", "P01732"),
  stringsAsFactors = FALSE
)
mapped <- map_genes_to_features(gs, mapping,
  gene_col = "Gene", feature_col = "Protein")
lengths(mapped)
}
