% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hallmark.R
\name{validate_hallmark}
\alias{validate_hallmark}
\title{Validate custom immune gene sets against MSigDB Hallmarks}
\usage{
validate_hallmark(
  irps_obj,
  expr_mat,
  gene_map,
  gene_col = "Gene",
  feature_col = "FeatureID",
  species = "Homo sapiens",
  category = "H",
  immune_hallmarks = NULL,
  min_set_size = 3L
)
}
\arguments{
\item{irps_obj}{An \code{irps} object returned by \code{\link{compute_irps}}.}

\item{expr_mat}{A numeric expression matrix (features × samples). Must
be the same matrix used for \code{\link{compute_irps}}.}

\item{gene_map}{A data.frame mapping gene symbols to feature IDs.}

\item{gene_col, feature_col}{Column names in \code{gene_map} for gene symbols
and feature IDs.}

\item{species}{Character, species identifier for msigdbr. Default: "Homo sapiens".}

\item{category}{Character, MSigDB category. Default: "H" (Hallmark).}

\item{immune_hallmarks}{Character vector of Hallmark gene set names to
use. If \code{NULL}, a default set of 12 immune-related Hallmarks is used.}

\item{min_set_size}{Integer, minimum number of mapped features to retain
a Hallmark set. Default: 3.}
}
\value{
A list with components:
\itemize{
\item \code{correlation_matrix}: Spearman correlation matrix (custom × Hallmark).
\item \code{hallmark_scores}: Matrix of Hallmark ssGSEA scores.
\item \code{hallmark_stats}: Data frame of Hallmark scores by group (if Subtype
is available).
}
}
\description{
Computes ssGSEA scores using MSigDB Hallmark gene sets and correlates
them with the custom immune gene set scores from an \code{irps} object.
This provides external validation that the custom gene sets capture
biologically meaningful immune signals.
}
\examples{
\dontrun{
result <- compute_irps(expr, gene_map, platform = "proteomics")
val <- validate_hallmark(result, expr, gene_map)

# View strongest correlations
cor_mat <- val$correlation_matrix
which(cor_mat == max(cor_mat, na.rm = TRUE), arr.ind = TRUE)
}
}
